services:
  - type: web
    name: laravel-ci-cd-demo
    plan: free
    branch: main
    rootDir: .
    dockerfilePath: Dockerfile
    runtime: docker
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: DB_CONNECTION
        value: sqlite
